# browser-os

A simulated desktop environment in the browser, written in React/TypeScript.

Live demo: https://desktop.robhoffmann.me/

This repository contains the source code for the live Browser OS demo. It builds with Vite and
deploys to GitHub Pages via GitHub Actions (see `.github/workflows/deploy-gh-pages.yml`).
